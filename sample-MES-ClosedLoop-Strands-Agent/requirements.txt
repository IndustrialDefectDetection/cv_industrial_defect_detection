# Core dependencies
streamlit>=1.42.0
pandas>=2.2.0
numpy>=1.24.0
plotly>=6.0.0
python-dotenv>=1.0.0
reportlab>=4.0.0
fastapi>=0.100.0
requests>=2.31.0
pyarrow==24.0.0
uvicorn>=0.38.0

# Database generator
Faker>=22.0.0
SQLAlchemy>=2.0.0

# PostgreSQL driver — setupdatabase.py imports psycopg2 to build mescopy_v1,
# and the pipeline's bridge/analyze_batch seam talks to the same database.
# The -binary wheel avoids needing local Postgres build tooling.
psycopg2-binary>=2.9.9

# Agent framework with direct Anthropic/Claude support
strands-agents[anthropic]
strands-agents-tools
anthropic

# AWS support - keep because your email tool still uses boto3 / SES
boto3[crt]>=1.41.0

# Optional: keep only if you still use Bedrock AgentCore deployment
bedrock-agentcore<=0.1.5
bedrock-agentcore-starter-toolkit==0.1.14

# Optional utility
uv